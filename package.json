{
  "name": "demografia-de-empresas",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "validate:data": "node scripts/validate-data.mjs",
    "build": "npm run validate:data && bash scripts/build.sh"
  }
}

